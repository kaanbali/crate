{
  "name": "crate-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that fronts MusicBrainz, Cover Art Archive, ListenBrainz and Discogs for Crate",
  "scripts": {
    "dev": "wrangler dev --port 8787 --local",
    "deploy": "wrangler deploy",
    "health": "curl -s http://localhost:8787/health"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "allowScripts": {
    "workerd@1.20260815.1": true,
    "esbuild@0.28.1": true
  }
}
